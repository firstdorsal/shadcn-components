# Domain for Traefik TLS configuration
DOMAIN=vindelicum.eu

# Subdomain prefix (e.g., "picker" for picker.vindelicum.eu)
SUBDOMAIN=PROJECT_NAME

# Project name used for container naming and registry path
PROJECT_NAME=PROJECT_NAME

# Container registry URL (defaults to hub.vindelicum.eu in docker-compose.yaml)
# CI_REGISTRY=hub.vindelicum.eu

# Registry password for CI/CD (set in CI variables, not here)
# CI_REGISTRY_PASSWORD=
